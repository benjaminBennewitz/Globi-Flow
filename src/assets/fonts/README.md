# Fonts

Diese Dateien bitte lokal aus dem bestehenden Projekt übernehmen:

```text
inter-variable.ttf
material-symbols-outlined-latin-fill-normal.woff2
```

Die App nutzt bewusst nur Inter als Text- und Datenfont. Zahlen werden über `font-variant-numeric: tabular-nums` sauber ausgerichtet.
