/* src/app/app.routes.ts */

/**
 * @file Definiert die initialen Routen der App.
 * @module appRoutes
 */

import { Routes } from '@angular/router';

/** Initiale Routing-Tabelle für den Projektstart. */
export const routes: Routes = [];
